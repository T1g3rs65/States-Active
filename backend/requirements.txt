# States-Active backend runtime dependencies (current versions).
# Install: python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
#
# NOTE: openai is pinned to 1.x — the code uses the AsyncOpenAI +
# chat.completions.create interface, which changed in the 2.x/3.x SDKs.

fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.4
motor==3.7.1
pymongo==4.17.0
mongomock-motor>=0.0.29
openai==1.99.9
python-dotenv==1.2.1
